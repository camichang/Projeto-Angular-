export class Tarefa {
    constructor(
        public id?: number,
        public nome?: string,
        public concluida?: boolean
    ){

    }
}

// ? não são obrigatorios para a criacao do objeto, para deixar o elemento mais flexivel
//é o modelo de 